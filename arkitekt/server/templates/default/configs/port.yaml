db:
  db_name: {{setup.port.db_name}}
  engine: django.db.backends.postgresql
  host: db
  password: {{setup.postgres_password}}
  port: 5432
  username: {{setup.postgres_user}}
django:
  admin:
    password: {{setup.admin_password}}
    username: {{setup.admin_username}}
  debug: false
  hosts:
  - '*'
  secret_key: {{setup.port.django_secret_key}}
lok:
  issuer: lok
  key_type: RS256
  public_key: {{setup.jwt_key_pair.public_key}}{% if setup.static_tokens %}
  static_tokens: {% for token, user in setup.static_tokens.items() %}
    {{token}}: 
      sub: {{user.sub}}{% endfor %}{% endif %}
minio:
  access_key: {{setup.port.bucket.access_key}}
  buckets:
   media: portmedia
  host: minio
  port: '9000'
  protocol: http
  secret_key: {{setup.port.bucket.secret_key}}
redis:
  host: redis
  port: 6379
virtualizer:
  engine: docker
deployment:
  name: {{setup.name}}
