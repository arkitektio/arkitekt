mutation reserve(
  $node: ID!
  $template: ID
  $params: ReserveParamsInput
  $title: String
  $imitate: String
  $appGroup: ID
  $reference: String
  $provision: ID
) {
  reserve(
    node: $node
    template: $template
    params: $params
    title: $title
    imitate: $imitate
    provision: $provision
    appGroup: $appGroup
    reference: $reference
  ) {
    ...Reservation
  }
}

mutation unreserve($id: ID!) {
  unreserve(id: $id) {
    ...Reservation
  }
}
