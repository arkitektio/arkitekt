mutation reserve(
  $node: ID
  $template: ID
  $params: ReserveParamsInput
  $title: String
  $callbacks: [Callback]
) {
  reserve(
    node: $node
    template: $template
    params: $params
    title: $title
    callbacks: $callbacks
  ) {
    reference
    id
    status
    statusmessage
  }
}
