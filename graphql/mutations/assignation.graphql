mutation assign($reservation: ID!, $args: [AnyInput]!, $reference: String) {
  assign(reservation: $reservation, args: $args, reference: $reference) {
    ...Assignation
  }
}

mutation unassign($assignation: ID!) {
  unassign(assignation: $assignation) {
    ...Assignation
  }
}
