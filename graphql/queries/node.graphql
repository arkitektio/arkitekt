query get_node(
  $id: ID
  $package: String
  $interface: String
  $template: ID
  $q: String
) {
  node(
    id: $id
    package: $package
    interface: $interface
    template: $template
    q: $q
  ) {
    ...Node
  }
}
